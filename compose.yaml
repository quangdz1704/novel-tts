services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: novel_tts
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - novel_tts_postgres:/var/lib/postgresql/data

volumes:
  novel_tts_postgres:
